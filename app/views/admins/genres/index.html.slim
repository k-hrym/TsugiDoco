
.row
  h3 ジャンル一覧
  =link_to 'ジャンルを追加',new_admins_genre_path,class:"btn btn-primary ml-auto mb-2"
  table.table.table-bordered.back__white
    thead.thead-dark
      tr
        th ID
        th ジャンル名
        th 有効無効
        th
    tbody
      - @genres.each do |genre|
        tr
          td= genre.id
          td= genre.name
          td= genre.valid_invalid
          td= link_to '編集', edit_admins_genre_path(genre),class:"btn btn-outline-secondary"
