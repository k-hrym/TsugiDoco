// 削除対象のフォームを消す
$(".spot__form-<%= @spot.id %>").remove();

// SPOT- の後に続く番号を1から順に振り直す。
jQuery(function(){
  spotOrder();
});

// autocomplete機能ができるようにする。
jQuery(function(){
  placeAutoComplete();
});